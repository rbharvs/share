# Supply-chain floor: refuse to install any package version published less than
# 3 days ago (npm's min-release-age is in DAYS). Enforced on every npm
# install/ci, locally and in CI. See the repo's 72h dependency-pin policy.
min-release-age=3
