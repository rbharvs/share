# share infra (workspace placeholder)

Pulumi TypeScript managing AWS + Cloudflare. Data infra (DynamoDB + S3) lands in
**slice 12**, compute + CDN (Lambda + API Gateway + CloudFront/OAC + ACM) in
**slice 13**, and Cloudflare + production deploy wiring in **slice 14**. This
directory reserves the workspace in the monorepo scaffold landed by slice 01.
