# share frontend (workspace placeholder)

Vite + React + TypeScript + Tailwind + shadcn/ui dashboard SPA. The real
skeleton (static serving, dev proxy, library view) is built in **slice 09**;
upload/publish interactions in **slice 10**. This directory reserves the
workspace in the monorepo scaffold landed by slice 01.
